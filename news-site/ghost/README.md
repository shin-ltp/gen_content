# Ghost VPS 部署配置样例

把 `docker-compose.yml` 和 `Caddyfile` 上传到 VPS 的 `/opt/ghost/`，改掉其中的 `CHANGE_ME_*` 和 `news.example.com` 后 `docker compose up -d`。

详细手顺见 [../docs/ghost-setup.md](../docs/ghost-setup.md)。